name: slsa-provenance

on:
  workflow_dispatch:
  release:
    types: [created]

permissions:
  contents: read
  id-token: write

jobs:
  provenance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build (release)
        run: |
          cargo build --release
      # Reference provenance generator. Customize to your org's policy.
      - name: Generate provenance (reference)
        run: |
          echo "Configure SLSA provenance generation here (org-specific)."
